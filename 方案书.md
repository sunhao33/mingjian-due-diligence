# 明鉴 · 企业财务尽调与风险研判 Agent

> **赛道**：Boundless Agents（无界应用）— AI+金融
> **一句话定位**：上传一份标的公司财报，智能体自动完成解析、指标计算、规则匹配与风险研判，输出一份可解释的尽调初筛报告。

---

## 1. 项目概述

**明鉴**是一个面向**投资尽调初筛**场景的财务尽调智能体。投资经理上传标的公司财报（PDF / 图片 / 扫描件），Agent 通过多步编排自动完成：

1. **资料理解** —— 解析三大报表（资产负债表、利润表、现金流量表），抽取为结构化数据；
2. **指标计算** —— 计算偿债、盈利、营运、成长、现金流五大类财务指标；
3. **规则匹配** —— 将指标与风控规则库、行业基准比对，命中异常项；
4. **风险提示** —— 对命中项做归因分析，给出带**证据引用**的风险结论；
5. **投研整理 / 流程辅助** —— 汇总为一份结构化尽调报告，并可导出 Markdown / PDF。

**核心价值**：把「靠人工翻财报 + 凭经验判断」的低效、非标、易漏的风险研判过程，变成一条**可运行、可复现、可解释**的自动化任务闭环。

---

## 2. 背景与行业场景价值

### 2.1 行业痛点

| 痛点 | 现状 |
|------|------|
| 项目源多、初筛重 | 投资团队面对大量 BP 与财报，财报是尽调第一道关，人工通读 + 建表需数小时/份 |
| 标准不一 | 不同分析师的指标口径、判断标准不一，初筛结论难复现、难对齐 |
| 易遗漏 | 财务粉饰、收入质量、现金流背离、商誉减值等风险分散在报表各处，肉眼易漏 |
| 门槛高 | 早期标的财务不规范，年轻投资经理经验不足，判断依赖个人积累 |

### 2.2 目标用户

聚焦 **投资机构与投研人员的标的初筛尽调**：

- **一级市场股权投资机构**：对拟投项目的财务健康度做快速初筛，判断是否值得进入深度尽调；
- **二级市场投研 / 分析师**：财报速读、风险筛查，辅助投资研究决策；
- **FA / 财务顾问**：为项目方准备材料时自检财务风险，提升材料质量。

### 2.3 价值主张

把资深投资经理的财报尽调经验，沉淀为**可配置规则库 + 可解释推理链**，把「财报初筛」从小时级人工通读变成**分钟级、标准化、可追溯**的流程——帮投资团队更快筛掉问题标的、聚焦值得深挖的项目。这正是 AI Agent 进入投资尽调现场、提升效率的落点。

---

## 3. 产品定义与任务闭环

### 3.1 交互流程（Demo 主路径）

```
投资经理上传标的公司财报
      ↓
【解析】文档/表格抽取 → 三大报表结构化 JSON
      ↓
【计算】pandas 计算五大类财务指标
      ↓
【匹配】规则引擎 + 行业基准库 → 命中风险规则
      ↓
【研判】LLM 对命中项归因 → 风险提示（带指标、阈值、实际值、证据）
      ↓
【输出】结构化尽调报告（评分 + 风险清单 + 结论 + 免责声明）
```

### 3.2 与赛题五个验证点的映射

| 赛题验证点 | 本方案对应能力 |
|-----------|---------------|
| 资料理解 | 三大报表结构化抽取（文本 + 视觉双通道） |
| 规则匹配 | 可配置风控规则引擎 + 行业基准库比对 |
| 风险提示 | 命中项归因分析，输出带证据引用的风险清单 |
| 投研整理 | 自动汇总为结构化尽调报告 |
| 流程辅助 | 多步编排工作流，替代人工通读建表 |

### 3.3 不是「泛问答」

本方案**不依赖**用户逐条提问，而是端到端执行一条**固定的尽调工作流**；计算与规则判定由代码保证，LLM 只做「理解 + 归因 + 表达」，因此结果可复现、可审计。

---

## 4. 技术方案

### 4.1 总体架构

采用 **LangGraph 状态机编排**的多步 Agent 工作流，模块间以结构化数据（JSON）传递，而非自由文本：

```
┌─────────────────────────────────────────────────────────┐
│                      LangGraph 编排层                      │
│   解析 → 抽取 → 计算 → 规则匹配 → 风险研判 → 报告生成        │
└──────┬──────────┬──────────┬──────────┬──────────────────┘
       │          │          │          │
  文档解析层   结构化抽取   指标计算   规则引擎/知识库
 (pdfplumber   (DeepSeek    (pandas,  (可配置规则库 +
  /Qwen-VL)    JSON输出)    纯代码)    行业基准 RAG)
```

### 4.2 各模块设计

**① 文档解析层**
- 文本型 PDF：`pdfplumber` / `PyMuPDF` 提取文字与表格；
- 扫描件 / 复杂表格：视觉模型（Qwen-VL）OCR 读表；
- 输出：三大报表的结构化草稿。

**② 结构化抽取**
- 用 LLM（DeepSeek，JSON 输出模式）将解析文本抽取为统一的报表 Schema（科目 → 金额），多期可比对。

**③ 指标计算（纯代码，保证准确与可复现）**
- 偿债能力：流动比率、速动比率、资产负债率、利息保障倍数
- 盈利能力：毛利率、净利率、ROE、ROA
- 营运能力：应收账款周转率、存货周转率、总资产周转率
- 成长能力：营收增长率、净利润增长率
- 现金流质量：经营现金流 / 净利润、自由现金流

**④ 规则引擎 + 行业基准库**
- 可配置规则库，每条规则含：指标、阈值/条件、风险等级、解释模板。示例：
  - 应收账款 / 营收 占比过高 → 收入质量风险
  - 经营现金流持续为负且与净利润背离 → 利润质量风险
  - 资产负债率超阈值 → 偿债风险
  - 「存贷双高」（货币资金高 + 有息负债高）→ 财务粉饰嫌疑
  - 商誉占净资产比例过高 → 减值风险（投资尽调重点关注）
  - 关联交易占营收比异常 → 收入独立性风险
  - 审计意见非标准无保留 → 重大风险
- 行业基准：按行业（制造 / 商贸 / 金融等）提供可比区间。

**⑤ 风险研判（LLM 归因）**
- 输入：命中规则 + 对应指标实际值 + 阈值 + 报表上下文；
- 输出：每条风险的**解释与证据引用**（明确引用哪张表、哪个科目、哪个值）。

**⑥ 报告生成**
- 汇总为结构化尽调报告：综合评分、风险清单（分级）、关键指标表、结论与免责声明；
- 导出 Markdown / PDF。

### 4.3 技术选型

| 层 | 选型 | 说明 |
|----|------|------|
| 编排框架 | LangGraph（Python） | 开源、表达多步工作流、易可视化 |
| 主推理模型 | DeepSeek（deepseek-chat / deepseek-reasoner） | 云端 API、中文强、成本低，权重开源 |
| 视觉/表格解析 | Qwen-VL（可选） | 处理扫描件与复杂表格；DeepSeek 无视觉，此层需补充视觉模型 |
| 指标计算 | Python + pandas | 数值计算不交给 LLM，保证准确性 |
| 知识库 | 向量检索（行业基准 / 监管规则） | 规则可插拔、可扩展 |
| Demo UI | Streamlit / Gradio | 快速可演示，上传文件 → 展示报告 |

> **关于「开源」**：主推理模型采用有开源权重的 DeepSeek / Qwen，编排与计算全部为开源框架，代码可整体开源，满足赛道「开源模型 + 开源工具」要求。

### 4.4 Agent 能力体现

- **工具调用**：解析、计算、规则匹配、检索均封装为可调用工具（Tool），由编排层调度；
- **结构化状态**：各步骤以 Schema 校验后的 JSON 流转，状态可追踪、可审计；
- **可解释性**：每条风险结论可回溯到「指标 + 阈值 + 实际值 + 报表位置」的证据链。

### 4.5 当前实现进度与真实数据验证

原型已可运行（命令行 `python main.py` + Streamlit Demo `python -m streamlit run app.py`），并已在**两份真实 A 股年报**上跑通全流程：

| 标的 | 行业 | 综合风险等级 | 命中关键风险 |
|------|------|-------------|-------------|
| 贵州茅台（2024 年报） | 白酒 | 低风险 | 未命中显著规则 |
| 万科 A（2024 年报） | 房地产 | 高风险 | 经营亏损、利息保障倍数过低、毛利率下滑、营收负增长 |

验证要点：

- **指标准确性**：茅台毛利率 91.9%、净利率 52.3%、ROE 36.9%，与公开数据一致；万科净利润 -487 亿、营收同比 -26.3%，与年报披露吻合。
- **行业基准生效**：万科资产负债率 73.7%，在房地产 85% 参考线内不误报，同时因经营亏损、营收下滑等被正确判为高风险。
- **规则库覆盖**：已实现 8 大类 13+ 条规则（偿债、现金流、收入、盈利、成长、资产减值、财务粉饰、合规），含行业基准（房地产 85% / 建筑 80% / 白酒食品 50% / 金融豁免）。
- **计算与推理分离**：财务指标由纯 Python 计算，有息负债、派生比率等不交给 LLM 计算，保证准确与可复现；规则引擎支持 `--no-llm` 纯规则模式。

> 说明：LangGraph 编排、Qwen-VL 扫描件通道、行业基准 RAG 尚未接入，当前为线性 Pipeline 等价实现，复赛阶段补齐。

---

## 5. 创新点

1. **计算与推理分离**：财务指标用代码精确计算，LLM 只做归因与表达——避免大模型「算错数」，同时兼顾准确性与可复现性；
2. **可解释风险研判**：每一条风险提示都携带可追溯证据链，而非「黑盒打分」；
3. **可配置规则库 + 行业基准**：经验规则化、规则开源化，支持按行业、按机构扩展；
4. **双通道资料理解**：文本 + 视觉，覆盖普通 PDF 与扫描件。

---

## 6. 数据来源与合规

- **数据来源**：以**公开的上市公司财报**（交易所披露、年报/半年报 PDF）为主，无数据授权与隐私门槛；用户自有材料为补充。
- **隐私保护**：数据本地处理；除调用模型 API 外不向第三方传输原始材料；报告中不含个人敏感信息。
- **风险提示与免责声明**：报告明确标注「本报告由 AI 辅助生成，仅供研究参考，不构成投资建议」。
- **行业边界**：定位为「尽调初筛辅助」而非「投资决策」，最终判断由投资团队完成。

---

## 7. 开放复用

- 代码、规则库、抽取 Schema 开源；
- 抽取「财务指标计算」「规则引擎」「报告模板」为**可复用组件**，供同类场景二次开发；
- 提供样例数据与一键部署说明，降低复现门槛。

---

## 8. 迭代计划

| 阶段 | 目标 | 交付物 |
|------|------|--------|
| 初赛 | 验证场景价值与可行性 | 作品简介、方案 PPT/PDF、可选原型 |
| 复赛 | 完成可运行 Demo 与技术验证 | 更新方案、Demo、运行说明、代码/工程材料 |
| 决赛 | 现场路演与答辩 | 路演 PPT、现场 Demo、最终工程材料 |

---

## 9. 团队

- **团队规模**：个人 / 小团队（1–3 人）
- **算力与资源**：无本地 GPU，采用云端开源模型 API（DeepSeek / Qwen），轻量部署、低成本复现。
- **分工**：
  - 方案设计与财务规则：________
  - 工程实现与 Agent 编排：________
  - Demo 与演示材料：________
- **相关经验**：（补充：财务/投研背景、Agent 开发经验等）

> 待补充成员姓名与具体分工。

---

## 10. 风险与边界

- **模型解析准确性**：复杂财报表格的抽取可能存在误差，需人工复核机制兜底；已通过「科目归一化 + 派生科目代码计算」降低 LLM 算术错误风险。
- **指标口径差异**：不同行业/准则下的口径需在规则库中显式声明；行业基准库需持续扩充（当前覆盖房地产 / 建筑 / 白酒食品 / 金融豁免）。
- **视觉模型依赖**：扫描件 OCR 依赖 Qwen-VL，需确认阿里云百炼 API 可用性；Demo 主路径暂以文本型 PDF 为主。
- **审计意见识别**：意见类型位于「审计报告」段落，页面定位需向前覆盖；对非标准意见的判定依赖提示词约束，仍需人工抽查。
- **合规边界**：定位于「尽调初筛辅助」，非投资建议；仅基于公开披露数据，不涉及内幕信息。
- **模型幻觉风险**：LLM 归因可能产生不实表述，通过「规则先行、LLM 仅归因 + 证据链回溯」缓解，并保留 `--no-llm` 纯规则模式。
